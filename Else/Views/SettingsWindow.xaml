<Window x:Name="Settings" x:Class="Else.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:tabs="clr-namespace:Else.Views.SettingsTabs"
        mc:Ignorable="d"
        Title="Settings" Width="1000" Height="600" ResizeMode="NoResize">

	<Grid>
		<TabControl Padding="0 1" SelectedIndex="1">
			<TabControl.Resources>
				<Style TargetType="ListViewItem">
					<Setter Property="Padding" Value="10 3" />
					<Style.Resources>
						<!-- Foreground for Selected ListViewItem -->
						<SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="Black" />
						<!-- Background for Selected ListViewItem -->
						<SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#1C070580" />
					</Style.Resources>
				</Style>

				<Style TargetType="Border">
					<!--<Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>-->
				</Style>
			</TabControl.Resources>

			<TabItem Header="General" />
			<TabItem Header="Theme">
				<tabs:ThemesTab x:Name="ThemesTab" />
			</TabItem>
		</TabControl>

	</Grid>
</Window>