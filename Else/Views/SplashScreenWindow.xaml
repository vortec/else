<Window x:Class="Else.Views.SplashScreenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStyle="None"
        UseLayoutRounding="True"
        AllowsTransparency="True"
        Topmost="True"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Title="Else First Install Splash!"
        Background="#1a1a1a"
        Height="250"
        PreviewKeyDown="Window_PreviewKeyDown"
        Width="356">
    <Border BorderBrush="#3b3b3b" BorderThickness="2">
        <StackPanel>
            <Image Margin="35" Source="pack://application:,,,/Else;component/Resources/AppIcon256.png" Width="70"
                   HorizontalAlignment="Center" />
            <TextBlock Foreground="#dedede" FontSize="14" HorizontalAlignment="Center" TextAlignment="Center"
                       RenderOptions.ClearTypeHint="Enabled">
                Else is now running...  <LineBreak /> Press CTRL + SPACE to activate
            </TextBlock>
            <Button HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="25" Padding="10 2"
                    Click="Button_Click">
                Ok
            </Button>
        </StackPanel>
    </Border>
</Window>