

essentials:
	applications
	preferences (control panel stuff?)
	files (documents, images, contacts)
web search:
	full url
	plugins
		google
		pictures of
		maps
		translate {}
		gmail
		gmail {}

calculator
dictionary
shell (>)
system:
	recycle bin
	logout
	sleep
	lock
	restart
	shut down




done
----
3. resultslist wrap around (when pressing down key on the last result)

fabio feedback:
--------------

4. rename wildcard to AlwaysDisplay
5. document and fix tokenmatch
6. remove TokenMatch.Wildcard
7. rename issingular
8 rename tokenComplete, token
9. enforce method,variable naming convention (uppercase, see parse())
10. Plugin.AlwaysDisplay 
11. change event system?



naming convention:
- Types use PascalCase
- properties and methods always use PascalCase
- public members (fields, consts) use PascalCase
- local variables use camelCase
- parameters use camelCase
- private fields use camelCase
- private fields that back a property prefix a _




check these:
------------
switch from 3rd party NotifyIcon to pure .NET version from the winforms namespace
hotkey unregistration?